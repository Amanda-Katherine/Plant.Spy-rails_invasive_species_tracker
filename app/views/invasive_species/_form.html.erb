<%= form_with(model: @invasive_species) do |f| %>
    <%= f.label :common_name %>
    <%= f.text_field :common_name %>

    <%= f.label "Defining Characteristics: growth pattern, origin history, environment preference, range, etc" %>
    <%= f.text_area :description %>

    <%= f.submit %>
<% end %>
    
    